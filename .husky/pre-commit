#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Block BOM in JSON files; attempt to fix, then check again
node scripts/remove-bom.js --fix >/dev/null 2>&1 || true
node scripts/remove-bom.js --check
